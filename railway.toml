[build]
builder = "NIXPACKS"
buildCommand = "cd gaoncare-ai && corepack enable && corepack prepare npm@latest --activate && npm install"
startCommand = "cd gaoncare-ai && npm start"

[env]
PORT = "3000"
